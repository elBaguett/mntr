replicaCount: 1
#here replace latest tag with smth like {{tag received from build pipeline}}
image:
  repository: lebonkdoge/mntr
  tag: frontend-3390331670a218bca3175e50558eb157557bd65d
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: frontend.local
      paths:
        - /
  tls: []

config:
  backendHost: backend
  backendPort: 8080

resources: {}