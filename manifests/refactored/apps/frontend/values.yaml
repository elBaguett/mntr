replicaCount: 1
#here replace latest tag with smth like {{tag received from build pipeline}}
image:
  repository: lebonkdoge/mntr
  tag: frontend-fe6da01ff41673ec1390c37b00f640102c956f54
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: frontend.local
      paths:
        - /
  tls: []

config:
  backendHost: backend
  backendPort: 8080

resources: {}