apiVersion: v1
kind: PersistentVolume
metadata:
  name: maildata-pv
spec:
  capacity:
    storage: 400Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/pv/maildata
